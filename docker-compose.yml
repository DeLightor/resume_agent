services:
  resume-agent:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ~/.resume-agent:/root/.resume-agent
      - ~/.cache:/root/.cache
    env_file: .env
    environment:
      - HF_ENDPOINT=https://hf-mirror.com
    restart: unless-stopped
